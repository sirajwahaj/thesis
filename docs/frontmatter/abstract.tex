% =============================================================================
% frontmatter/abstract.tex — Sammanfattning / Abstract
% =============================================================================

\chapter*{Sammanfattning}
\addcontentsline{toc}{chapter}{Sammanfattning}

% TODO: Write the abstract last, when you know your results.
% Max ½ page. Summarise the topic, method, and most important conclusions.

This thesis investigates how reliability, system stability, and execution performance change when a workflow orchestration system migrates from a single virtual machine using a Docker executor (where each pipeline job runs as a Docker container) to a Kubernetes cluster using isolated pod execution. Dagster is used as a representative modern pipeline orchestration framework. Through three controlled experiments across six concurrency levels, the study identifies a \emph{crossover point}---the specific concurrent workload level at which the Kubernetes deployment becomes net beneficial despite its inherent scheduling overhead. Experiments are executed on local infrastructure (a Linux VM and a Kind cluster) to ensure full control, reproducibility, and zero budget dependency, with an optional cloud validation phase on Google Kubernetes Engine if resources permit. Results are intended to provide engineering teams with data-backed guidance for VM-to-Kubernetes migration decisions in pipeline orchestration contexts.

\vspace{1em}
\noindent\textbf{Keywords:} Kubernetes, Dagster, workflow orchestration, resource contention, pod isolation, DockerRunLauncher, crossover point, Kind, reliability measurement, DevOps

\clearpage
