% =============================================================================
% 1.6 Research Hypothesis
% =============================================================================

\section{Research Hypothesis}
\label{sec:hypothesis}

Migrating workflow orchestration execution from a single-VM Docker executor to a Kubernetes Run Launcher will produce measurable reliability improvements through pod isolation, but will also introduce measurable scheduling latency and execution overhead beyond the container startup cost that already exists in the VM Docker deployment.

These advantages are conditional on workload level. At low concurrency, the VM is expected to outperform Kubernetes on execution speed because resource contention has not yet reached critical levels and there is only Docker container startup overhead to pay, without the additional Kubernetes pod scheduling overhead. At high concurrency, Kubernetes is expected to outperform the VM as pod isolation and per-pod resource limits prevent the contention collapse that a single Docker daemon cannot avoid when all containers share the same host.
