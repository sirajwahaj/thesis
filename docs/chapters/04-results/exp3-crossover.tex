% =============================================================================
% 4.3 Experiment 3 Results — Overhead and Crossover Point (SQ3, SQ4)
% =============================================================================

\section{Experiment 3 Results --- Overhead and Crossover Point (SQ3, SQ4)}
\label{sec:results-exp3}

Experiment~3 is derived from the Experiment~1 and~2A datasets: no new runs are required.
Pod scheduling latency and container startup time are extracted from the pod lifecycle timestamps collected during Experiment~2A.
Net execution time differences between the two environments are computed from the respective dagster run timestamps.

\subsection{Kubernetes Scheduling Overhead (SQ3)}
\label{subsec:results-overhead}

\begin{table}[H]
  \centering
  \caption{Kubernetes scheduling overhead by concurrency level.
  Values are means over 3 repetitions $\times$ $k$ concurrent pods per level.
  Startup overhead = total pod lifetime $-$ Dagster execution time (joined on \texttt{run\_id}).}
  \label{tab:overhead-summary}
  \small
  \begin{tabular}{clccc}
    \toprule
    \textbf{Level} & \textbf{Concurrent Jobs} & \textbf{Pod Sched. Latency (s)} & \textbf{Startup Overhead (s)} & \textbf{K8s Total (s)} \\
    \midrule
    L1  & 1  & $<$1 & 4.3  & 67.7 \\
    L2  & 2  & $<$1 & 4.6  & 68.7 \\
    L3  & 3  & $<$1 & 5.1  & 69.7 \\
    L4  & 5  & $<$1 & 6.8  & 74.9 \\
    L5  & 7  & $<$1 & 11.6 & 92.2 \\
    L6  & 10 & $<$1 & 14.9 & 96.5 \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Pod scheduling latency.}
In a single-node Kind cluster with pre-pulled images and no resource quotas that could cause pending states, all pods were scheduled within less than 1\,second.
This is consistent with the theoretical minimum for a lightly loaded local cluster.

\paragraph{Container startup time.}
The dominant overhead component is \emph{container startup time}: the interval from pod submission until the Dagster job process begins executing.
This is measured as total pod lifetime (submitted\_ts to container exit) minus the Dagster execution duration (\texttt{end\_time} $-$ \texttt{start\_time} from dagster\_runs.csv), joined on \texttt{run\_id}.
At L1 this yields 4.3\,s, rising to 14.9\,s at L6.
This growth reflects increased Python initialisation queuing and PostgreSQL handshake contention when many containers start concurrently on the single-node cluster.

Note that the pod scheduling latency itself is essentially zero ($<$1\,s) on a single-node Kind cluster with pre-pulled images, so the startup overhead is dominated by container initialisation: Python interpreter start, dependency import, and the Dagster gRPC handshake with the code server.
This contrasts with the $\approx$25\,s median scheduling latency reported by \citet{choi2021} for production GKE clusters where multi-node scheduling decisions dominate.

\subsection{The Crossover Point (SQ4)}
\label{subsec:results-crossover}

\begin{table}[H]
  \centering
  \caption{Net execution time comparison: VM vs.\ Kubernetes.
  VM mean is for \emph{surviving} jobs only.
  K8s total = Dagster execution time + startup overhead (pod lifetime $-$ exec time, joined on \texttt{run\_id}).
  $\Delta = \text{K8s total} - \text{VM mean}$: negative means K8s is faster.
  At L3+ the VM success rate falls below 95\,\%, making raw speed comparison misleading.}
  \label{tab:crossover}
  \small
  \begin{tabular}{clcccc r}
    \toprule
    \textbf{Level} & \textbf{Jobs} & \textbf{VM SR} & \textbf{VM Mean (s)} & \textbf{K8s SR} & \textbf{K8s Total (s)} & \textbf{$\Delta$ (s)} \\
    \midrule
    L1  &  1 & 100.0\% & 66.7 & 100.0\% & 67.7 & $+$1.0 \\
    L2  &  2 & 100.0\% & 67.3 & 100.0\% & 68.7 & $+$1.4 \\
    L3  &  3 &  66.7\% & 92.9 & 100.0\% & 69.7 & $-$23.2 \\
    L4  &  5 &  40.0\% & 181.6 & 100.0\% & 74.9 & $-$106.7 \\
    L5  &  7 &  38.1\% & 92.6 & 100.0\% & 92.2 & $-$0.4 \\
    L6  & 10 &  30.0\% & 111.3 & 100.0\% & 96.5 & $-$14.8 \\
    \bottomrule
  \end{tabular}
  \par\smallskip
  \footnotesize\textit{SR = success rate. Negative $\Delta$ = K8s faster on survivor exec time alone. At L3--L6 the VM is unreliable ($<$95\,\% SR); K8s is the only viable option regardless of the $\Delta$ sign.}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{sq3_k8s_overhead.png}
  \caption{Kubernetes scheduling overhead analysis (SQ3). Left: total startup overhead per concurrency level (mean $\pm$ std across 3 reps). Right: overhead breakdown stacked with job execution, compared to VM baseline. Overhead grows from 4.3\,s at L1 to 14.9\,s at L6 due to concurrent initialisation contention on the single-node Kind cluster.}
  \label{fig:k8s-overhead}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{sq4_crossover_point.png}
  \caption{Crossover analysis (SQ4): cost per successful job delivery and reliability divergence across concurrency levels.
  Both the reliability crossover (VM below 95\,\%) and the performance crossover
  (K8s total time below VM effective time accounting for reruns) occur at L3 (3 concurrent jobs).}
  \label{fig:crossover}
\end{figure}

\paragraph{Reliability crossover (Metric 11).}
The VM's job success rate declined sharply from 100\,\% at L1--L2 to 66.7\,\% at L3, 40.0\,\% at L4, 38.1\,\% at L5, and 30.0\,\% at L6.
The \emph{reliability crossover} threshold --- defined as the concurrency level at which VM success rate drops below 95\% --- was crossed between L2 (100\%) and L3 (66.7\%).
This places the reliability crossover at \textbf{L3 (3 concurrent jobs)}.
This result directly answers SQ1: the VM begins to degrade significantly at 3 concurrent jobs.

\paragraph{Performance crossover (Metric 12).}
The performance crossover is more nuanced than a simple monotone comparison.
K8s execution time itself \emph{increases} with concurrency---from 63.4\,s (L1) to 80.6\,s (L5)---because the single-node Kind cluster experiences node-level resource contention at higher concurrency despite per-pod limits.
Adding the measured startup overhead (4--15\,s, growing with concurrency), K8s total wall-clock is:
L1 $\approx$68\,s, L2 $\approx$69\,s, L3 $\approx$70\,s, L4 $\approx$75\,s, L5 $\approx$92\,s, L6 $\approx$97\,s.

Compared to VM survivor-only execution times (66.7\,s at L1, 67.3\,s at L2, 92.9\,s at L3, 181.6\,s at L4, 92.6\,s at L5, 111.3\,s at L6), K8s is \emph{slightly slower} on raw survivor time at L1--L2 ($+$1.0\,s, $+$1.4\,s) but \emph{faster} from L3 onward ($-$23.2\,s at L3, $-$106.7\,s at L4, $-$0.4\,s at L5, $-$14.8\,s at L6).
The raw wall-clock performance crossover (K8s faster than VM survivors) occurs at \textbf{L3}.
However, the VM's 38.1\,\% success rate at L5 means 62\% of jobs fail outright---a comparison of survivor execution times understates the true cost of VM operation.

\paragraph{Combined crossover point.}
The combined crossover --- the concurrency level where Kubernetes becomes net beneficial considering both reliability and execution time --- is \textbf{L3 (3 concurrent jobs)}.
At this level: (1) the VM drops below 95\,\% success rate (66.7\,\%) while Kubernetes maintains 100\,\%, and (2) when the VM failure rate is factored in as a re-execution cost, the VM effective time per successful delivery ($\approx$139\,s at L3) already exceeds the K8s total wall-clock ($\approx$70\,s), making K8s 70\,s faster per successfully delivered job.
The 4--15\,s pre-execution startup overhead per run represents 7--18\,\% of total K8s time; however, this cost is outweighed at L3+ by the VM's inability to reliably complete jobs at all.
At higher concurrency (L4--L5), K8s continues to deliver 100\,\% reliability despite pod startup overhead growing to 7--12\,s; the VM continues to fail at 38--40\,\% rates.
Both environments experience node-level pressure at L6 (10 concurrent jobs) on this 8\,GB single-node Kind cluster, though Kubernetes maintained 100\,\% reliability.

