% =============================================================================
% 3.5 Workload Definition
% =============================================================================

\section{Workload Definition}
\label{sec:workloads}

Workloads are defined precisely to ensure reproducibility and to simulate realistic pipeline execution resource consumption patterns. Six concurrency levels provide sufficient data points to identify the degradation curve and crossover region.

\begin{table}[H]
  \centering
  \caption{Defined workload levels.}
  \label{tab:workloads}
  \small
  \begin{tabularx}{\textwidth}{clccX}
    \toprule
    \textbf{Level} & \textbf{Concurrent Jobs} & \textbf{Per-Job CPU} & \textbf{Per-Job Memory} & \textbf{Job Description} \\
    \midrule
    L1        & 1  & 0.25 & 256 MB & Baseline: generates and sorts 500K random integers \\
    L2        & 2  & 0.25 & 256 MB & Light concurrency \\
    L3        & 3  & 0.50 & 512 MB & Moderate: reads 50\,MB CSV, performs transformations, writes output \\
    L4        & 5  & 0.50 & 512 MB & Heavy concurrency with same per-job profile \\
    L5        & 7  & 1.00 & 1 GB   & Stress: computes SHA-256 hashes of 5M strings sequentially \\
    L6 (Spike) & 10 & 0.50 & 512 MB & Burst exceeding single-node capacity \\
    \bottomrule
  \end{tabularx}
\end{table}

Each workload level is run \textbf{3 times} per environment. Results are reported as mean and standard deviation. For levels near the expected crossover (L4, L5), an additional 2 runs may be added if time permits to improve confidence.

\paragraph{Workload design rationale.}

\begin{itemize}
  \item L1--L2 establish the no-contention baseline where VM is expected to be faster.
  \item L3--L4 represent the transition zone where contention begins.
  \item L5--L6 represent the stress zone where VM collapse is expected.
  \item Both CPU-bound and memory-bound work patterns are included because they trigger different failure modes: CPU contention degrades performance gradually while memory exhaustion causes sudden termination.
\end{itemize}
