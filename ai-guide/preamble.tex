% ==============================================================
%  Preamble — Using AI as a Professional
% ==============================================================

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[a4paper, top=2.5cm, bottom=2.5cm, left=3cm, right=2.5cm]{geometry}
\usepackage{microtype}
\usepackage[dvipsnames,svgnames,table]{xcolor}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{enumitem}
\usepackage{parskip}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage[most,breakable]{tcolorbox}
\usepackage[colorlinks=true,
            linkcolor=NavyBlue,
            urlcolor=NavyBlue,
            citecolor=NavyBlue,
            pdftitle={Using AI as a Professional},
            pdfauthor={}]{hyperref}

% --------------------------------------------------------------
%  Colours
% --------------------------------------------------------------
\definecolor{chapterblue}{RGB}{0,80,160}
\definecolor{tipgreen}{RGB}{20,120,60}
\definecolor{warnred}{RGB}{185,28,28}
\definecolor{infoblue}{RGB}{13,110,210}
\definecolor{conceptpurple}{RGB}{100,16,220}
\definecolor{lightbg}{RGB}{248,249,252}

% --------------------------------------------------------------
%  tcolorbox environments
% --------------------------------------------------------------
\tcbuselibrary{skins,breakable}

\newtcolorbox{tipbox}[1][Tip]{
  enhanced, breakable,
  colback=green!5!white, colframe=tipgreen,
  coltitle=white, colbacktitle=tipgreen, fonttitle=\bfseries\small,
  title={#1}, boxrule=0.8pt, arc=4pt,
  left=8pt, right=8pt, top=5pt, bottom=5pt,
  before skip=8pt, after skip=8pt,
}

\newtcolorbox{warnbox}[1][Warning]{
  enhanced, breakable,
  colback=red!5!white, colframe=warnred,
  coltitle=white, colbacktitle=warnred, fonttitle=\bfseries\small,
  title={#1}, boxrule=0.8pt, arc=4pt,
  left=8pt, right=8pt, top=5pt, bottom=5pt,
  before skip=8pt, after skip=8pt,
}

\newtcolorbox{infobox}[1][Note]{
  enhanced, breakable,
  colback=blue!4!white, colframe=infoblue,
  coltitle=white, colbacktitle=infoblue, fonttitle=\bfseries\small,
  title={#1}, boxrule=0.8pt, arc=4pt,
  left=8pt, right=8pt, top=5pt, bottom=5pt,
  before skip=8pt, after skip=8pt,
}

\newtcolorbox{conceptbox}[1][Concept]{
  enhanced, breakable,
  colback=violet!4!white, colframe=conceptpurple,
  coltitle=white, colbacktitle=conceptpurple, fonttitle=\bfseries\small,
  title={#1}, boxrule=0.8pt, arc=4pt,
  left=8pt, right=8pt, top=5pt, bottom=5pt,
  before skip=8pt, after skip=8pt,
}

% --------------------------------------------------------------
%  Chapter / Section formatting
% --------------------------------------------------------------
\titleformat{\chapter}[display]
  {\normalfont\Huge\bfseries\color{chapterblue}}
  {\chaptertitlename~\thechapter}{16pt}{\Huge}
  [\vspace{4pt}{\color{chapterblue}\titlerule[2pt]}\vspace{6pt}]

\titlespacing*{\chapter}{0pt}{-10pt}{20pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\color{chapterblue!90!black}}
  {\thesection}{1em}{}

\titleformat{\subsection}
  {\normalfont\large\bfseries\color{chapterblue!70!black}}
  {\thesubsection}{1em}{}

% --------------------------------------------------------------
%  Header / Footer
% --------------------------------------------------------------
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\itshape\leftmark}
\fancyhead[R]{\small\thepage}
\setlength{\headheight}{14pt}
\renewcommand{\headrulewidth}{0.4pt}
\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% --------------------------------------------------------------
%  List settings
% --------------------------------------------------------------
\setlist[itemize]{topsep=4pt, itemsep=2pt, leftmargin=*}
\setlist[enumerate]{topsep=4pt, itemsep=2pt, leftmargin=*}

% --------------------------------------------------------------
%  Q&A command
% --------------------------------------------------------------
\newcounter{qnum}[chapter]
\newcommand{\qa}[2]{%
  \refstepcounter{qnum}%
  \vspace{8pt}%
  \noindent\textbf{\color{chapterblue}\theqnum.\ #1}\par%
  {\leftskip=1.5em\relax\noindent#2\par}%
  \vspace{2pt}%
}
