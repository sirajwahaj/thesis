\section{Discussion of \sq{1} --- The VM Failure Threshold}

\textit{[To be completed after results are collected.]}

This section will discuss the measured failure threshold against resource contention theory
from Pillar~1. It will address whether the collapse was non-linear as predicted by
\citet{nanda1991} and at what concurrency level it occurred. It will also discuss whether
the result was surprising and what it implies for teams currently running similar workloads
on a \vm{}.
